# Laba11mas

### Условие задачи

5. С одномерным массивом, состоящим из n вводимых с клавиатуры положительных и отрицательных целых чисел выполнить следующие вычисления: Произведение элементов массива, расположенных между первым и вторым нулевыми элементами.

### Алгоритм

1. **Начало**

2. Задать исходные данные:

  `N` - кол-воэл. в массиве

5. Ввести `N ` элементов с клавиатуры.

7. Найти эл. массива = 0:

  `for (int i = 0; i < N; i++) {`
  
	`if (M[i] == 0) {`
  
		`if (z1 == -1) {`
    
			`z1 = i;`
      
		`}`
    
		`else  {`
    
			`z2 = i;`
      
			`break;`

5. Найти результат произведений:
    
		for (int i = z1 + 1; i < z2; i++) {

			rez = rez * M[i];
   
		}

8. Вывести результат:

`printf("Произведение элементов - %d", rez);`

### Блок-схема 
!![Блок-схема алгоритма](Labafor8.png)

### 2. Реализация программы

    #include <stdio.h>
    
    #include <stdlib.h>
    
    #include <locale.h>
    
    #include <math.h>
    
    #define _USE_MATH_DEFINES 
    
    #define _CRT_SECURE_NO_DEPRECATE
    
    //#define N 5

    int main() {
    
	  setlocale(LC_CTYPE, "RUS");
    
	  int N;
    
	  printf("Введите размер массива: \n");
    
	  scanf("%d", &N);
    
	  int *M = malloc(N * sizeof(int));
    
	  printf("Введите %d целых чисел: \n", N);
    
	  for (int i = 0; i < N; i++) {
    
		  scanf("%d", &M[i]);
      
	  }
    
	  int z1 = -1;
    
	  int z2 = -1;
    
	  for (int i = 0; i < N; i++) {
    
		  if (M[i] == 0) {
      
			  if (z1 == -1) {
        
				  z1 = i;
          
			  }
        
			  else  {
        
				  z2 = i;
          
				  break;
          
			  }
        
		  }
      
	  }
	
	  int rez = 1;
    
	  for (int i = z1 + 1; i < z2; i++) {
    
		  rez = rez * M[i];
      
	  }
    
	  printf("Произведение элементов - %d", rez);
    
	  return 0;
    
    }

### 3. Контрольный пример

    Пусть N = 5, тогда массив включает 5 элементов.
    
    Возьмём: 0 2 2 0 1
    
    Произведение между 0 элементами - 2*2=4

### 4. Результаты работы программы

    Введите размер массива:
    
    5
    
    Введите 5 целых чисел:
    
    0 2 2 0 1
    
    Произведение элементов - 4

### 5. Информация о разработчике

Гусев Иван бИЦ-251
